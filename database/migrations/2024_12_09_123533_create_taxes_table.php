<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('taxes', function (Blueprint $table) {
            $table->id();
            $table->string('taxname')->nullable(); // Allow taxname to be nullable
            $table->string('subtitle')->nullable(); // Subtitle as string
            $table->json('percentage')->nullable(); // Use JSON for multiple percentages
            $table->decimal('total', 10, 2)->nullable(); // Total amount
            $table->timestamps();
        });
    
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('taxes');
    }
};
